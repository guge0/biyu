# 笔驭当前交接

更新时间：2026-08-23

## 新对话启动纪律

新对话开始任何代码动作前，必须按顺序：

1. 读取本文件。
2. 读取仓库根目录 `AGENTS.md`。
3. 执行 `git status --short` 和 `git log -5 --oneline`。
4. 根据“未完成”和“下一步”继续，不重复已经完成的工作。

本文件进入版本库；它比聊天记录和被忽略的本地交接文件更权威。

## 当前状态

- 开发仓：`biyu-dev`，用于开发和验收。
- 正式仓：`biyu`，用于作者正式使用。
- 开发版和正式版不是两套产品；开发版用于验收，正式版用于发布。
- 最新开发提交：`52b986a`。
- 完整自动化测试：`1733 passed，38 deselected`。
- 用户级浏览器全走查：未完成。
- 最近一次推送：网络失败，需先检查远端状态后再推送。

## 已完成

- 作者显式新建人物卡：姓名必填、重名拒绝、其余字段可空。
- 新建人物卡取消按钮和设定集样式。
- 开发服务加载当前源码，开发版可自动重启自己的旧服务。
- 正式版保留安全冲突保护，不自动杀未知服务。
- 安装包 smoke 测试修复。
- 防手滑副本保留策略修复。
- 责编启动器在找不到 Claude Code 时明确进入缺失分支。
- 润色默认关闭，作者模型面板不展示润色。

## 未完成任务

1. 启动流程统一：启动、安装、发布分离；作者不手动切换端口或仓库。
2. 责编多会话：支持继续最近对话、选择历史对话、开始新对话；每本书可有多个 session。
3. 更新提示：正式版检测已发布版本，在右上角显示更新红点和版本信息。
4. 8090 用户级全走查：设定集空态、新建人物卡、取消、重名、责编启动和失败提示。
5. 工作台顶部按中枢裁定分层。
6. 防手滑副本默认路径绑定当前运行数据根。

## 交付规则

- 代码通过自动化测试不等于用户级走查完成。
- `commit`、`push`、正式仓同步、服务重启必须分别记录。
- 未 push 或未部署不得写成已发布。
- 每次停止前更新本文件的当前状态、失败项和下一步。
