# Biyu 协作与验收规则

## 完成前必须验证

- 任何需求在报告“已完成”之前，必须先执行与变更风险相匹配的验证；只读分析、静态检查或单元测试不能自动替代用户实际使用路径的验收。
- 涉及启动器、安装、端口、浏览器页面、文件选择器或 Windows 行为时，必须在 Windows 环境实际执行对应入口，验证成功、失败和重复操作等关键路径。
- 涉及 UI 时，至少要检查页面真实加载、关键交互和错误状态；涉及数据写入、删除或备份时，必须使用隔离的临时数据验证结果，并确认没有触碰生产数据。
- 测试未执行、执行失败、只验证了开发副本而没有验证用户实际入口，均视为需求未完成。最终回复必须如实说明未验证项，不得把“代码已改”表述为“功能已完成”。
- 若生产副本与开发副本分离，必须明确说明验证的是哪一个目录，并在交付前验证更新后的生产使用路径，或明确记录尚未同步的阻塞项。

## 交付说明

- 最终回复应列出实际运行的验证命令或用户路径、结果和剩余风险。
- 发现验证不足时，先补验证或报告阻塞，不得仅凭推断收口。

## 开发与生产目录

- `E:\webnovel\biyu-dev` 是唯一开发工作区。代码修改、自动化测试和隔离验收只在这里进行；验收必须使用临时数据目录和用户配置目录，不得连接正式书稿。
- `E:\webnovel\biyu` 是用户日常使用的生产副本，正式数据默认位于 `%USERPROFILE%\BiyuData`。不得把未提交文件从开发目录手工复制到生产目录作为发布方式。
- 标准发布顺序固定为：开发目录修改 -> 隔离测试 -> 提交 -> 推送 -> 生产目录拉取 -> 安装刷新 -> 从生产入口做冒烟验收。
- 在生产冒烟验收通过前，只能说“开发完成/待发布”，不得说“用户可用”或“正式完成”。
