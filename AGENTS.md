# 项目交付规则

- 每次涉及作者可见功能的修改，交付前必须做一次按真实用户路径的全走查：启动对应环境，打开页面，执行主流程，确认成功、失败和空状态；不能只依赖单元测试。
- 测试必须覆盖本次改动的后端接口和前端入口；无法完成用户级走查时，必须在交付说明中明确写出原因，不得称为已验证。
- Git commit 使用简洁、中文、能说明结果的标题，避免冗长英文描述。
- 交付说明必须明确区分“已提交”和“已发布”：commit 不等于 push，push 不等于部署；未完成的阶段不得写成已发布。
- 停止工作前必须检查任务是否可由新对话恢复：把目标、未完成项、最近提交、测试结果、阻塞原因和下一步写入交接记录；不能只留在聊天上下文里。
- 新对话开始任何代码动作前，必须先读取版本库中的 `docs/交接/TL当前交接.md`；该文件是唯一有效的当前任务入口。读取后再检查 `git status` 和最近提交，不能依赖被忽略的本地记录。
